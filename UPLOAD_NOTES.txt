APPLY VAULT — READY TO UPLOAD

Main gate:
  /apply-vault.html

Private content:
  /apply-vault-private.html

What works immediately on GitHub Pages:
- The gate is now a minimal single-email screen with hacker-style motion graphics.
- mehrsh3d@gmail.com is already approved as the owner account.
- Any unknown email shows ACCESS DENIED and opens the request form automatically.
- The request form generates a ready-to-send email addressed to mehrsh3d@gmail.com.
- Visitors can choose OPEN EMAIL APP or OPEN GMAIL WEB.
- The email subject and body are pre-filled from the form.
- There is no longer an AUTH NODE OFFLINE error when no Apps Script URL is configured.

Optional Google Sheet / automatic email copy:
- access-config.js intentionally leaves apiUrl blank, so the page does not depend on Apps Script.
- If you later deploy google-apps-script.gs as a Web App and paste its /exec URL into apiUrl, the same gate can also check ALLOW statuses in Google Sheets and send a server-side copy of each request.

Old URLs remain preserved as redirects:
  /application-journey.html -> /apply-vault.html
  /application-journey-private.html -> /apply-vault-private.html

IMPORTANT SECURITY NOTE:
This is still a lightweight client-side gate on GitHub Pages. It is suitable for portfolio/restricted presentation access, but not for highly sensitive documents.
